package parser;

import data.StackTrace;

public class Parser {

	public StackTrace parse(String filepath) {
		// TODO: parse
		int bucket = 0;
		StackTrace stack = new StackTrace(bucket);
		return stack;
	}
}
